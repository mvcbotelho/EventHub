meta {
  name: List Events
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/events?page=0&size=20
  body: none
  auth: inherit
}

headers {
  accept: application/json
}

docs {
  Lists all events (paginated). Optional filters: title, location, startFrom, startTo.
}
