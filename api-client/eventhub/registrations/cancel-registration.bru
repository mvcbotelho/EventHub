meta {
  name: Cancel Registration
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/events/{{eventId}}/registrations/me
  body: none
  auth: inherit
}

headers {
  accept: application/json
}

docs {
  Cancels the authenticated user's registration for an event.
}
